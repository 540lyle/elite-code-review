#!/usr/bin/env bash
# Sync skill files to .codex/ and .claude/ after checkout
REPO_ROOT="$(git rev-parse --show-toplevel)"
command -v powershell.exe >/dev/null 2>&1 && powershell.exe -ExecutionPolicy Bypass -File "$REPO_ROOT/sync-skills.ps1"
